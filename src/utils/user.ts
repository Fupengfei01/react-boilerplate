/**
 * Test whether the current user is a logged in user.
 *
 * @returns true if the user already logged in, false otherwise.
 */
export function isLogin(): boolean {
  // TODO: Add your logic here.
  return true;
}
